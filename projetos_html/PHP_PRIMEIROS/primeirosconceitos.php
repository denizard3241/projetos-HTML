<?php

// Atribuição
$valor = 10; // Atribui o valor 10 à variável $valor.

// Aritméticos
$soma = 5 + 3; // Adiciona 5 e 3, resultando em 8.
$subtracao = 10 - 4; // Subtrai 4 de 10, resultando em 6.
$multiplicacao = 6 * 2; // Multiplica 6 por 2, resultando em 12.
$divisao = 20 / 4; // Divide 20 por 4, resultando em 5.
$modulo = 10 % 3; // Calcula o resto da divisão de 10 por 3, resultando em 1.

// Comparação
$igual = (10 == 10); // Verifica se 10 é igual a 10, resultando em verdadeiro (true).
$diferente = (5 != 3); // Verifica se 5 é diferente de 3, resultando em verdadeiro (true).
$maior = (8 > 5); // Verifica se 8 é maior que 5, resultando em verdadeiro (true).
$menor = (4 < 7); // Verifica se 4 é menor que 7, resultando em verdadeiro (true).
$maior_ou_igual = (6 >= 6); // Verifica se 6 é maior ou igual a 6, resultando em verdadeiro (true).
$menor_ou_igual = (9 <= 10); // Verifica se 9 é menor ou igual a 10, resultando em verdadeiro (true).

// Lógicos
$e_logico = true && false; // Verifica se ambas as condições são verdadeiras, resultando em falso (false).
$ou_logico = true || false; // Verifica se pelo menos uma das condições é verdadeira, resultando em verdadeiro (true).
$nao_logico = !true; // Inverte o valor lógico, resultando em falso (false).

// Concatenação de strings
$nome = "João";
$saudacao = "Olá, " . $nome . "!"; // Concatena a saudação com o nome, resultando em "Olá, João!".

// Incremento e decremento
$contador = 0;
$contador++; // Incrementa o valor do contador em 1.
$contador--; // Decrementa o valor do contador em 1.

?>
